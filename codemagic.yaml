workflows:
  ios-workflow:
    name: Too Rich iOS Workflow
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: "Too Rich CI"
    environment:
      groups:
        - supabase_credentials
      vars:
        BUNDLE_ID: "com.kaif.toorich"
        XCODE_WORKSPACE: "ios/TooRichApp.xcworkspace"
        XCODE_SCHEME: "TooRichApp"
      node: latest
      xcode: latest
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.kaif.toorich
    scripts:
      - name: Generate .env from secrets
        script: |
          echo "SUPABASE_URL=$SUPABASE_URL" >> .env
          echo "SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY" >> .env
          cat > src/generatedConfig.ts << 'EOF'
          export const SUPABASE_URL = "PLACEHOLDER_URL";
          export const SUPABASE_ANON_KEY = "PLACEHOLDER_KEY";
          EOF
          sed -i '' "s|PLACEHOLDER_URL|$SUPABASE_URL|" src/generatedConfig.ts
          sed -i '' "s|PLACEHOLDER_KEY|$SUPABASE_ANON_KEY|" src/generatedConfig.ts
      - name: Install npm dependencies
        script: |
          npm install
      - name: Install CocoaPods dependencies
        script: |
          cd ios && pod install
      - name: Debug bundle id
        script: |
          xcodebuild -workspace "$XCODE_WORKSPACE" -scheme "$XCODE_SCHEME" -showBuildSettings -configuration Release | grep -i "PRODUCT_BUNDLE_IDENTIFIER\|PRODUCT_NAME"
      - name: Set build number
        script: |
          cd ios && agvtool new-version -all 1
      - name: Set up export options
        script: |
          xcode-project use-profiles --project "ios/TooRichApp.xcodeproj" -v
      - name: Build ipa for App Store distribution
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      email:
        recipients:
          - kaif39882@gmail.com
        notify:
          success: true
          failure: true
      app_store_connect:
        auth: integration
        submit_to_testflight: true